[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
rust-codecs-core = { path = "../core", features = ["identity", "rot13", "base64", "json"] }

[[bin]]
name = "cli"
path = "src/main.rs"
