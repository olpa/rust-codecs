[package]
name = "rust-codecs-io"
version = "0.1.0"
edition = "2021"

[dependencies]
rust-codecs-base = { path = "../base" }
compcol = { version = "0.6", default-features = false, features = ["std"] }

[lib]
name = "rust_codecs_io"
path = "src/lib.rs"
